<!DOCTYPE html>
<html>
<head>
  <title>Exampad - Learn, Play, Succeed with personalized quizzes and gamified learning!</title>
  <%= stylesheet_link_tag 'application.tailwind', media: 'all' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <%= csrf_meta_tags %>
</head>

<body class="bg-gray-100">
  <div class="container mx-auto p-6">
    <header class="flex justify-between items-center mb-8">
      <h1 class="text-4xl font-bold text-blue-600">Exampad</h1>

      <div class="flex items-center">
        <% if user_signed_in? %>
          <span class="mr-4 text-gray-700">
            Logged in as: <strong><%= current_user.role.capitalize %></strong>
          </span>
          <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: "px-4 py-2 bg-red-500 text-white rounded-lg hover:bg-red-600" %>
        <% else %>
          <%= link_to 'Sign In', new_user_session_path, class: "px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600" %>
        <% end %>
      </div>
    </header>

    <%= yield %>
  </div>

<footer class="bg-gray-800 text-white text-center p-4 mt-6">
  <p>&copy; 2024 Exampad. All rights reserved.</p>
</footer>
</body>
</html>
