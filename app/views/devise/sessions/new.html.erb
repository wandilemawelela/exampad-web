<h2 style="font-size: 2em; margin-bottom: 1em;">Log in</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field" style="margin-bottom: 1em;">
    <%= f.label :email, style: 'display: block; font-weight: bold;' %>
    <%= f.email_field :email, autofocus: true, autocomplete: "email", style: 'width: 100%; padding: 0.5em; border: 1px solid #ccc; border-radius: 4px;' %>
  </div>

  <div class="field" style="margin-bottom: 1em;">
    <%= f.label :password, style: 'display: block; font-weight: bold;' %>
    <%= f.password_field :password, autocomplete: "current-password", style: 'width: 100%; padding: 0.5em; border: 1px solid #ccc; border-radius: 4px;' %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field" style="margin-bottom: 1em;">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me, style: 'font-weight: bold; margin-left: 0.5em;' %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "Log in", style: 'background-color: #4CAF50; color: white; padding: 0.5em 1em; border: none; border-radius: 4px; text-decoration: none; cursor: pointer;' %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
