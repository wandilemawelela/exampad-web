<!DOCTYPE html>
<html>
<head>
  <title>Leaderboard</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <style>
    table {
      width: 100%;
      border-collapse: collapse;
    }
    th, td {
      padding: 10px;
      border-bottom: 1px solid #ddd;
    }
    th {
      background-color: #4A90E2;
      color: white;
    }
    tr:nth-child(even) {
      background-color: #f2f2f2;
    }
  </style>
</head>
  <main style="padding: 20px;">
    <h1>Leaderboard</h1>
    <table>
      <thead>
        <tr>
          <th>#</th>
          <th>Email</th>
          <th>Points</th>
        </tr>
      </thead>
      <tbody>
        <% @students.each_with_index do |student, index| %>
          <tr>
            <td style="padding: 10px; border-bottom: 1px solid #ddd;"><%= index + 1 %></td>
            <td style="padding: 10px; border-bottom: 1px solid #ddd;"><%= student.email %></td>
            <td style="padding: 10px; border-bottom: 1px solid #ddd;"><%= student.points %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </main>

  <footer>
    <p>&copy; <%= Date.today.year %> Exampad. All rights reserved.</p>
    <p>Powered by <%= link_to 'Rails', 'https://rubyonrails.org', target: '_blank' %></p>
  </footer>
</body>
</html>
